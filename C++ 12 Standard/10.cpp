//PROGRAM -- 10
/* Write a C++ program that invoke a function Newdate() to return
  a object of date type. the function Newdate() takes two parameters,
  an object olddate of date type and no. of days to calculate
  Newdate() as, old date + no. of days and returns the Newdate().  */

#include<iostream.h>

class DATE
